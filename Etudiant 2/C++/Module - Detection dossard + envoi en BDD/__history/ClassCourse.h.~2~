//---------------------------------------------------------------------------

#ifndef ClassCourseH
#define ClassCourseH
//---------------------------------------------------------------------------
class ClassCourse{
  private:
	  int IdCourse
	  int NbTours;
	  //std::map<int, Participant*> mapIdToParticipant;
  public:
	  ClassCourse();
	  ~ClassCourse();
	  int getIdCourse(int IdCourseChoisie);
	  int getNbTours(int NbTours);
	  void demarrerCourse(int IdCourse);
	  void stopperCourse(int IdCourse);
};
#endif
