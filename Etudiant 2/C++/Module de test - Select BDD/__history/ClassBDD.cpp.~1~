//---------------------------------------------------------------------------

#pragma hdrstop

#include "ClassBDD.h"
//---------------------------------------------------------------------------
#pragma package(smart_init)

ClassBDD::ClassBDD()
{

}

ClassBDD::~ClassBDD()
{

}

int ClassBDD::connectBDD()
{
	mySQL = mysql_init(NULL);

	connection=mysql_real_connect(mySQL, "127.0.0.1", "root", "root", "moduleselect", 0, NULL, 0);
	if(conn==NULL)
		{
			return 0;//ça ne fonctionne pas
		}
	else
		{
			return 1;//ça a fonctionné
		}

}